import { motion } from "motion/react";
import { Box, Paper, Stack } from "@mui/material";

const BoxM = motion.create(Box);
const StackM = motion.create(Stack);
const PaperM = motion.create(Paper);

export { BoxM, StackM, PaperM };
